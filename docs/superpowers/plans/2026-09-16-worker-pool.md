# 常驻 Worker 五路调度（阶段十一）

接续 b4ed6ee。目标为本地可部署的五路 Worker，不开通资源、不改生产变量，不宣称主机已部署。

1. 新增先失败测试：有界并发五路、故障隔离、收到停止信号后不领取新任务，等待在途任务正常收尾。
2. 在现有 BatchWorker 租约/步骤锁之上增加运行池，每个执行槽独立数据库连接；CLI --concurrency 1–5（默认 5）。--once 保持一次一个步骤语义。
3. 真实 PG：创建五个工作区并原子启动，之后完全不调用浏览器/run，用独立连接池执行完整任务，一路模型失败、其他四文件落库、仅返一分；验证无重复模型请求及 graceful stop。
4. 运行增量 Worker/CLI/PG 测试，更新部署说明与检查点。

生产限制：仍需用户确认持续运行 Python 的主机，使用同一数据库/主密钥与同一 Git 版本；初始单实例部署，禁止未经容量验证自动扩容。当前网页与 Worker 共享同一批次步骤锁；浏览器遇到其他执行器认领时不重复发送，用户可刷新状态。Worker 主机未确认前不伪装“后台已常驻”。

## 本地阶段完成记录

2026-09-16：步骤 1–4 已完成。新增三个运行池单元测试先失败后通过，连同原两个 Worker 测试共五项通过；独立真实 PG 测试直接运行生产 `serve`，五路峰值、四文件/一退款、九次模型阶段无重复、SIGTERM 收尾均通过。另补测 CLI once、HTTP 步骤争用重排队、旧租约不能完成新认领，四项 PG/CLI 定向测试通过。最后只读检查 `--help` 和非法 `--concurrency 6`（退出 2）通过。沿用本机隔离测试库与固定上游响应，没有调用生产模型。

已内联审查独立连接、原步骤锁/租约复用、异常日志不输出私有内容、关闭期间不领取新任务、once 兼容。部署文档已更新到 schema v4 和五路命令。生产主机、旧版任务停稳、真实模型现场验收仍未确认，不推定上线成功。
