# 品牌详情页吉祥物替换设计

日期：2026-08-28  
状态：用户已确认

## 目标

将 `profile.html` 的“AI平台适配专题”标题区域右侧机器人替换为用户提供的冰雪精灵图片，并移除原图灰黑色背景，使人物以透明背景融入现有浅色网格卡片。

## 资源处理

- 输入文件：`E:\图片\零雪IP\第三期\ChatGPT Image 2026年8月2日 19_19_50 (4).png`
- 输出文件：`images/lxue-ice-elf-wave-transparent.png`
- 输出格式：带 Alpha 通道的 PNG。
- 保留人物主体、浅蓝头发、雪花发饰、尖耳、双手和白色服装的完整边缘。
- 删除灰黑色背景，不添加新背景、文字、边框、阴影或其他人物。
- 保持原图正方形画布和人物比例，不拉伸、不裁掉头发或手指。

## 页面接入

- 只修改 `profile.html` 中 `.ai-platform-robot` 的图片资源与替代文本。
- 桌面端继续使用现有 `158px × 158px` 展示框。
- 移动端继续使用现有 `96px × 96px` 展示框。
- 保持 `object-fit: contain`，确保人物完整显示。
- 复用现有投影与布局，不改变标题、文案、专题卡片和导航。
- 替代文本改为“零雪冰雪精灵介绍六大AI平台适配专题”。

## 验证

1. 检查输出 PNG 确实包含透明像素，四角背景已透明。
2. 在桌面宽度下确认人物位于原机器人区域内，无拉伸、溢出或遮挡文字。
3. 在移动端宽度下确认人物保持完整，标题区域正常换行。
4. 运行项目已有校验命令，确认资源引用和页面结构无回归。
5. 只提交本任务涉及的规格、图片、HTML 和必要样式，不提交工作区现有未跟踪文件。
6. 推送到 `kudisengjie/weijia` 的 `master`。

## 非目标

- 不修改其他页面的吉祥物。
- 不重做“AI平台适配专题”布局。
- 不修改六个平台卡片、平台 Logo 或文案。
- 不删除工作区现有未跟踪文件。
- 本任务完成并推送前不继续 Agnes 模型接入。
