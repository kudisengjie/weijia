# GEO SaaS 部署与验收接续

本说明对应 SaaS 五工作区发布版本（schemaVersion=4）。2026-09-16 用户已确认无在执行的任务，安排推送网页/API；暂无独立服务器，Worker 生产部署另行进行，不阻止网页版本发布。是否推送成功以 GitHub master 的提交号为准，是否部署以 EdgeOne 的部署记录为准。所有密码、API Key、主密钥和数据库连接串都只能放在服务端环境或密钥管理中，不得进入 Git、截图、日志或本文档。

## 已有 EdgeOne 配置保持不变

本阶段没有新增必须在 EdgeOne 填写的环境变量：

| 变量 | 用途 |
| --- | --- |
| APP_ORIGIN | 实际 HTTPS 官网来源，必须与浏览器请求一致 |
| GEO_ACCOUNT | 总账号 |
| GEO_PASSWORD_HASH | 现有 scrypt 格式的密码哈希 |
| GEO_MASTER_KEY | 现有 64 位十六进制加密主密钥，不要随意替换 |
| DATABASE_URL | Neon 提供的完整 PostgreSQL 连接串，保留 sslmode=require 等参数 |
| IMA_ADMIN_SECRET | 所有者更新 IMA 的管理口令 |
| IMA_OPENAPI_CLIENTID | 所有者 IMA Client ID，数据库未覆盖时使用 |
| IMA_OPENAPI_APIKEY | 所有者 IMA API Key，数据库未覆盖时使用 |

模型 API Key 仍由各用户在原设置界面填写。其密钥按账号加密存放在 PostgreSQL，退出登录不会删除。前端不会保存这些密钥。

部署构建仍使用项目根目录 geo-site、输出 dist、安装 npm ci、构建 npm run build。Python Cloud Functions 与静态输出分别打包。核对正式部署的 Git 提交号，不能仅凭 master 分支名确认已上线。

## 数据与缓存

启动迁移是幂等的，当前 schemaVersion 为 4；新增表和列由服务端初始化，不要手动删表修复环境。v4 增加加密工作区草稿、版本冲突和独立批次关联，详见 [五工作区服务端 v4](geo-workspaces-v4.md)。

IMA 检索、目录和原文缓存在同一个 PostgreSQL 数据库，由全站子账号共享，缓存正文加密。命中缓存不再请求 IMA；只有任务所需的目录、规则、问句证据会获取。企业上传资料、模型配置、批次和文章按用户隔离，普通成员不能调用总账号的缓存管理接口。

- 月度更新 IMA 凭据：总账号管理中心 → 共享 IMA；先验证新凭据，再保存，不清缓存。
- 主动清缓存：仅总账号可操作。增加缓存代数，新批次重新读取必要资料。
- 运行中批次固定创建时的缓存代数，不因清缓存或换月更换中途资料。旧代数据保留，不能直接删除正在使用的缓存。
- 缓存原文、模型密钥、文章文件及完整批次状态均加密保存；批次中的公司正文、规则、证据、草稿和旧版内联文章不再明文落库。

### 本次升级注意事项

1. 沿用原有 GEO_MASTER_KEY。旧库先完成 v3 批次加密迁移，再初始化 v4 工作区；重复初始化不会重新加密或扣分。账号、任务序号、文章文件和积分流水保留。
2. 部署前先暂停批次并等待当前调用完成，再发布前端/API；如已有 Worker，先正常停止，再用同一提交启动。不要混跑旧版明文写入程序或不了解五工作区名额的旧 API/Worker。数据库约束会拒绝旧程序写回明文；不能直接回退到 v1/v2 代码。
3. 加密内容绑定账号和批次 ID；跨账号读取或错置密文会被拒绝。主密钥丢失会导致历史内容无法解密，请在站点之外安全备份。IMA 的月度 API Key 更新不是 GEO_MASTER_KEY 轮换，两者不要混淆。
4. 如需轮换 GEO_MASTER_KEY，必须另行执行受控重加密迁移，不能只修改 EdgeOne 变量。本次不进行主密钥轮换。
5. 本次更新保护当前逻辑记录，不会抹除 Neon 历史备份、旧日志或已经导出的明文。保留的历史备份应继续限制访问，不自动删除用户数据。
6. v1 旧批次没有模型密钥快照和积分预扣记录：已有文章仍可下载，未结束批次可取消并解锁，但不会擅自使用当前密钥续跑。请取消旧批次后新建任务；不会补扣旧任务积分。

加密沿用 PostgreSQL pgcrypto 的 AES-256 方案，生产数据库连接保持 TLS；数据库服务和管理员仍属于信任边界，不是端到端加密。[PostgreSQL 官方说明](https://www.postgresql.org/docs/current/pgcrypto.html)

## 子账号、积分和有效期

总账号在独立“管理中心”的子账号目录创建成员，默认 30 天；初始积分为 0。必须先确认所选目标账号，再在积分/续期目录操作。普通成员没有管理中心入口，服务端管理接口也拒绝普通成员访问；成员可在个人设置查看自己的余额、最近 100 条流水、有效期和已有微信联系二维码。官网不处理在线支付。

一个有效 Excel 任务行预扣 1 积分，空格式行不计分。同一行要求多篇文章时，该行全部文章完整保存后才算成功。五行中一行失败，最终只保留四分消耗；其余行继续执行，失败行会显示原因。取消、到期或执行器中断会对未完整输出的行退款；重复请求不重复退款。

服务端完整文件保存后，单纯下载网络失败不退款，可以重新下载。当前文件格式是 MD。首次运行前选择本机输出目录并记住路径，按用户要求留待后续阶段。

所有者创建成员、修改订阅、积分调整、IMA 更新与清代均记录管理审计，记录中不含密码或密钥。续期重复提交相同起止日期不新增订阅。管理接口校验目标用户归属。

## A：EdgeOne 短步骤执行

每个 /api/batches/{id}/run 请求最多执行一个阶段，保存序号、任务状态和文件后返回。HTTP 请求中断不代表整个批次丢失。

- 暂停：/pause。服务端记录暂停意图，当前调用完成并保存后停止；保留预扣和模型锁。
- 继续：/resume。恢复暂停批次，不再次预扣。
- 取消：/cancel。停止后续步骤，返还未完成积分；已发出的模型请求无法撤回，提供商仍可能计费。
- 模型配置：账户有未结束批次时禁止更改，即使当前查看的是历史已完成批次。
- 中断：不自动重发结果不明的模型调用。超过 150 秒的失效步骤会跳过、退款并继续其他任务。
- 到期：不再发送模型请求，取消剩余任务并退款，已有文件继续可读。

没有独立 Worker 时，关闭网页后不会自动执行剩余步骤；重新打开可继续。不要把短步骤 API 等同于常驻后台。

## B：独立 Python Worker

已实现单进程最多五路调度，每个执行槽/步骤独立数据库连接，并用真实 PostgreSQL 在本地验证命令行执行、租约和无浏览器输出。生产仍需一个可持续运行 Python 的主机/容器；当前没有确认部署目标，不能声称 Worker 已在生产运行。不要擅自开通付费主机。

在该主机部署同一 Git 提交、复用上表的服务端环境变量，尤其 DATABASE_URL 和 GEO_MASTER_KEY 必须与 EdgeOne 相同。Worker 不需要单独开放公网端口。

在 geo-site 目录安装依赖，并按实际系统执行：

```sh
python3 -m venv .venv
.venv/bin/python -m pip install -r cloud-functions/requirements.txt
PYTHONPATH=cloud-functions .venv/bin/python -m geo_backend.worker --once
PYTHONPATH=cloud-functions .venv/bin/python -m geo_backend.worker --poll-seconds 2 --concurrency 5
```

Windows 已建虚拟环境时：

```powershell
$env:PYTHONPATH = (Resolve-Path './cloud-functions').Path
.venv/Scripts/python.exe -m geo_backend.worker --once
.venv/Scripts/python.exe -m geo_backend.worker --poll-seconds 2 --concurrency 5
```

`--once` 最多执行一个持久化步骤，不是执行整个批次，也不因指定并发而执行五步。`--concurrency` 允许 1–5、默认 5；这是单进程的总执行槽，不是账号名额。首轮部署单实例，不启用自动扩容，后续容量调整须另行验证数据库连接与供应商限流。

常驻模式处理后续待执行步骤，90 秒租约、心跳续租、令牌隔离过期 Worker；和浏览器共享同一批次步骤锁。网页遇到已认领步骤不重复发起，当前可手动刷新读取后台进度。进程异常由主机服务管理器重启，不启用“自动重试模型请求”。SIGINT/SIGTERM 停止领取新任务并等待在途阶段保存后退出；主机服务管理器须给足当前调用收尾时间，不能以立即强杀代替正常停止。

## 本地验收证据与剩余现场验收

已验证：

- 阶段十一 Worker：先失败后通过的并发/故障隔离/停止单元验证；真实 PostgreSQL 中运行实际 `serve`，无浏览器、无 `/run`，五个独立工作区峰值五路，四个完整 MD 文件落库，第五路仅退款一分，余额 10→6。五次生成和四次审核各只发送一次，SIGTERM 收尾退出并恢复信号处理器。上游仍为固定响应。CLI `--once`、步骤争用重排队、失效租约补测通过；帮助命令和 `--concurrency 6` 拒绝边界通过。
- 阶段十正式浏览器与真实 PG 已验证五份上传、各自模型、刷新恢复、CAS 冲突、并行启动、独立暂停继续、四份 MD 下载及第五失败退款；另覆盖保存途中再次编辑/切换、启动响应丢失后恢复、换账号资料清空、跨账号读取拒绝。未变化的测试不重复运行。
- 本轮加密改动后的真实 PostgreSQL 41 项通过，随后补充的生产默认 API 工厂检查 1 项通过（共 42 项），另有 schema/API/Worker 12 项定向检查通过。前一阶段未变化的 Python 60 项、Node 23 项和前端构建已有通过记录，未重复运行。
- IMA 固定响应驱动完整流水线，两个账号使用各自模型 Key；第二位账号零 IMA 请求，文章实际写入 PostgreSQL。
- 本机真实浏览器：总账号登录、指定子账号发积分及续期、创建子账号、流水查看、暂停/继续、下载真实存储的 MD 文件、模型解锁；普通成员不可见管理入口，手机 390px 不横向溢出。
- 浏览器发现的登录页重置默认日期问题已修复。PC 下拉框和日期字段已补充尺寸检查。
- 加密后再次使用真实浏览器验证暂停/继续、实际 MD 下载、模型解锁及成员权限；没有 JavaScript 错误。新增数据库检查覆盖旧进度迁移、明文写入拒绝、密文账号绑定、错误密钥和过期写入。
- 最后补齐 v1 未计费批次的只读/取消兼容：先复现旧批次取消失败，再完成修复，专属数据库测试、3 项新批次暂停/取消/部分退款检查及 7 项批次单元测试通过。

这些验证只使用隔离数据库和固定上游响应，不证明任何厂商线上 Key 的权限或余额正常。

发布与现场验收：

1. 批次正文副本加密及本地安全检查已完成。先按升级注意事项停止旧版正在执行的任务。
2. 保存发布检查点、推送授权的 GitHub master，再确认 EdgeOne 部署了完全相同的提交；不用重新填写现有变量。
3. 线上检查 /api/health 返回 schemaVersion=4、ready=true，再检查主动密码登录、五工作区、账户隔离、余额、IMA 配置状态和文章下载。health 不能证明 Worker 在运行。
4. 经确认后仅用一个测试任务验证真实 IMA 与已配置模型，核对实际调用次数和积分。不要批量尝试八家模型。
5. 确认独立 Worker 的实际承载主机，部署同一提交。只用经确认的一条测试任务，在启动后关闭网页，核对服务端文件完整保存、模型调用次数与积分，再回到网页下载文件；不能以本机固定响应测试代替这项现场验收。

本机浏览器夹具位于 tests_postgres/serve_ui_fixture.py；仅监听 127.0.0.1:8769，连接硬编码本机隔离 PostgreSQL（端口 55483），使用随机临时 schema，正常退出后清理。依赖 tests_postgres/requirements.txt 与已安装的 Playwright。截图和下载文件位于忽略的 output/playwright/saas-runtime，不进入 Git。
